"use strict";
var core_1 = require('./bootstrap/core');
var common_1 = require('../common/common');
var utils_1 = require('./bootstrap/utils');
var core_2 = require('../core/core');
function bootstrap(ngModule, component, providers) {
    if (providers === void 0) { providers = []; }
    // Bootstrap the core
    core_1.bootstrap(ngModule);
    // Bootstrap common components
    common_1.bootstrap(ngModule);
    // Bootstrap the injector
    utils_1.bootstrapHelper(ngModule, core_2.provide(core_2.Injector, { useFactory: function () { return new core_2.Injector(); } }));
    // Bootstrap providers
    providers.forEach(function (provider) { return utils_1.bootstrapHelper(ngModule, provider); });
    // Bootstrap the app tree
    utils_1.bootstrapHelper(ngModule, component);
}
exports.bootstrap = bootstrap;
